datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model DownloadHistory {
  id        String   @id @default(uuid())
  userId    String
  format    String
  createdAt DateTime @default(now())
}